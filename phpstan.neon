parameters:
  level: 8
  paths:
    - src
  excludePaths:
    - vendor
    # Tests use raw PDO + file() heavily; type-strict noise outweighs the
    # value. Keep production code at level 8 instead.
    - tests
  treatPhpDocTypesAsCertain: false
