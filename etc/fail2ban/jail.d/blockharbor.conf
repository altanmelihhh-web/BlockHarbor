# BlockHarbor — fail2ban jail
# Installed by bin/install.sh into /etc/fail2ban/jail.d/blockharbor.conf

[blockharbor]
enabled  = true
port     = 8443,https
filter   = blockharbor
logpath  = /var/log/blockharbor/app.log
maxretry = 5
findtime = 600
bantime  = 3600
backend  = auto
